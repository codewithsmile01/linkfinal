import React from 'react'
import { getAllPosts } from '@/lib/serveractions'


const Feed = async ({ user }: { user: any }) => {
    return (
        <div>

        </div>
    )
}

export default Feed
